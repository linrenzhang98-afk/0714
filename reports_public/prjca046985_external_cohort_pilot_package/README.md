# PRJCA046985 canonical planning package

This is the canonical namespace for PRJCA046985 expansion planning and source
inventory. The earlier files under `prjna1056765_external_cohort_pilot_package/`
are preserved historical artifacts; they are not execution authorization.

The current plan remains `PLANNING_ONLY_NOT_AUTHORIZED` until an ETYY-local,
read-only inventory verifies all 122 remaining FASTQ paths, bytes, and SHA256 values.
